// Escrve um programa que diga se o numero
//informa pelo usuario é par ou impar
//Imprima o numero e o resultado


const divRoot=  document.getElementById("root")
const numero=Number(prompt ('informe um número'))

const resto = % 2

console. log(resto)

let (resto ===0) {

    mensagem = '${numero}' é par'
} 
    else {
        mensagem= '$'

    }

    divRoot.appendChild(Documentnod)